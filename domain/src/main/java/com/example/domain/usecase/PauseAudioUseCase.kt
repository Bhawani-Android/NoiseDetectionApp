package com.example.domain.usecase

import com.example.domain.entity.AudioEntity
import com.example.domain.repository.AudioRepository
import javax.inject.Inject

//class PauseAudioUseCase @Inject constructor(private val audioRepository: AudioRepository) {
//    operator fun invoke(audio: AudioEntity) = audioRepository.pauseAudio(audio)
//}